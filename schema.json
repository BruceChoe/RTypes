// user schema
{
  "username": "user",
  "passwordHash": "saltedPasswordHash",
  "visualizations": [
    {}, {}, {} // insert visualization schema
  ]
}

// visualization schema
{
  "source_dataset": "sourceDatasetPath",
  "visualizations": [
    "alluvial.png",
    "heatmap.png"
  ]
}

// filesystem schema
// let's put user data in public for now
public
- <username>
  - data
    - <RData files, etc>
  - visualizations
    - <generated visualizations>